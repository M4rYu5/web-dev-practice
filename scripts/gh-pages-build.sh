# temp-build is the root folder of website
rm -r "temp-build"
mkdir "temp-build"
cp -r "../src/chat_preview" "temp-build/chat_preview"
cp -r "../src/pagination_practice" "temp-build/pagination_practice"
